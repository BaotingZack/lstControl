关于作业流程规范梳理：
1.状态介绍，
初始状态: 起重机位于 (x0, y0, z0)；
起始位置：物料所在位置(x1, y1, z1)；
目标位置：物料需要送达的目标位置(x2, y2, z2)；

2.业务流程：其中初始状态由定位算法获取，起始位置和目标位置由调度算法或UI界面获取。当获取到起始位置和目标位置并点击apply target后则开始执行整个调度流程。如果只给出起始位置或目标位置，另一个是空，则进行简单的点到点移动即可。
首先，由初始状态位置移动到物料所在的起始位置过程，X、Y和Z轴三轴同时联动，并且判断抓钩状态保证抓钩状态是开启（抓钩状态在话题/crane/cmd_vel/anguler的x轴信息，-1表示抓钩开启，1表示抓钩关闭。通过PLC函数GripperRelease和GripperClamp可以控制抓钩开关），但是Z轴需要先移动到安全高度（暂且默认这个阶段的安全高度1.0m），等X和Y到达目标位置后，等待1s左右使抓钩恢复静止，Z下降到目标位置，当X、Y和Z轴都到达指定位置，通过PLC控制抓钩夹取钢卷。当判断抓钩状态完全完成夹取货物后，延时0.5s左右保证安全。
然后，带货上升到安全高度（默认这个阶段的安全高度1.0m），然后X、Y和Z轴三轴同时联动，但是Z轴需要先移动到安全高度（默认这个阶段的安全高度1.5m），等X和Y到达目标位置后，等待1s左右使抓钩恢复静止，Z下降到卸货目标位置，当X、Y和Z轴都到达指定位置，通过PLC控制抓钩释放钢卷。当判断抓钩状态完全完成释放货物后，延时0.5s左右保证安全。
最后，抬升Z轴到1.6m位置高度，完成整个作业流程。
